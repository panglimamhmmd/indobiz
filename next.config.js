/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['panglimamuhammad.me', 'source.unsplash.com'], // Add your domain here
    },
};

module.exports = nextConfig;
